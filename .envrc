dotenv_if_exists .env

# Optional per-host overrides — git-ignored.
dotenv_if_exists .env.local

# Activate the uv-managed virtualenv so `alembic`, `pytest`
# etc. are on PATH without an explicit `uv run` prefix.
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi
